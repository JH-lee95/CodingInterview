n명으로 이루어진 원을 돌면서 j명이 남을 때 까지, m번째 사람을 제거하는 알고리즘 입니다.

--------------------------------------
예시 : n=[1,2,3,4,5,6] j=2 m=2 일 때
1,(2),3,(4),5,(6)
                1 (3) 5
--------------------------------------





##코드 시작##
def josephus(m,n,j):
    idx=-1
    while len(n)>j:
        idx += m
        if idx>len(n)-1:
            idx -= len(n)
        n.pop(idx)
        idx -=1
        print(n)
        
    return n
##코드 종료
